create table using: \. reCreateTable.sql

insert data with error(testing if constraints works): \. insertDatawithError.sql

show table contents: \. selectData.sql

create a view: \. view.sql

accessing the view: SELECT * FROM university_admission_requirement;
